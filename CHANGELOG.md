# Changelog

## v0.3.0 — 2026-04-27

### Added
- Manage your API key locally with `baseline config set api-key`, `baseline config get api-key`, `baseline config unset api-key`, and `baseline config path` — no need to export an environment variable for interactive use.

Operational follow-up: run the Release workflow manually with v0.3.0 after this PR is merged.
